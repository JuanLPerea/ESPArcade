#ifndef MENU_H
#define MENU_H

// Muestra la lista de juegos y no retorna nunca en uso normal:
// al elegir un juego lo ejecuta y, cuando ese juego termina,
// vuelve a mostrar el menú.
void menu_run(void);

#endif